[pytest]
markers =
    regression: mark a test as one that can fail for intentional change because it just tests regressions
    pickle: mark a test that ensures older pickle compatabilities. Should be removed when serialization is complete.
testpaths =
    .
